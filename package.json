{
  "name": "selfhosted_templates",
  "version": "1.0.0",
  "description": "Portainer Templates for Selfhosted Projects/Homelabs.",
  "private": true,
  "devDependencies": {
    "jsonlint": "^1.6.3",
    "yaml-validator": "^4.0.0",
    "glob": "^8.0.3"
  },
  "scripts": {
    "pretest": "jsonlint Template/template.json --quiet",
    "test": "node -e \"require('glob').sync('Template/Stack/*.yml').forEach(file => require('child_process').execSync('yaml-validator ' + file, {stdio: 'inherit'}))\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cgjgh/portainer_templates.git"
  },
  "author": "cgjgh",
  "license": "GPLv3"
}
